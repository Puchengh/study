hadoop fs -get /word /
1.客户端向namenode发送文件下载请求
2.namenode在元数据库中进行查询，如果查询到则返回客户端数据块以及副本的存储节点
    如果查询不到则会报错
3.返回到客户端的数据块的存储节点，就先回进行第一个数据块的下载
    进行数据下载的时候也是就近原则
4.第一个块下载成功之后 会生成一个crc文件，和上传的时候.meta文件进行文件完整校验 (起始偏移量和末尾偏移量中间的内容)
    如果检验通过则认为第一个块下载成功
5.进行第一个块的下载重复下载动作
6.所有的数据块下载成功只有，向namenode发送数据下载成功之后的响应.

文件下载过程中如果产生异常:数据块的某一个节点读取不到数据这个时候会向namenode进行汇报，namenode会将一个节点进行一个标记
，标记这个节点可能是问题节点，接着去读这个块存储的其他节点